namespace org.ingenious.mapper

entity org.ingenious.mapper.UserInfo

import java.util.List
import java.util.*


search selectById(id: Int, status: Status) {
    
    select * from user_info

    // where username = :username?
    // where username = :username?.notNull
    // where username = :username -> :username::notNull
    // where username = :username -> this::notNull
    // where username = 'test' -> :username:notNull
    // 
    // #if :username != null {
    //     where username = :username
    // }
    // 
    // #if :username::notNull {
    //     where username = :username
    // }
    
    
    
   
     
}
